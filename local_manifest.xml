<manifest>
  <!-- add github as a remote source -->
  <remote name="github" fetch="https://github.com" />

  <!-- add busybox from the cyanogenmod repository -->
  <project path="external/busybox" name="vmlinz/android_external_busybox" remote="github" revision="cm-11.0" />

  <!-- add libusb ported for android -->
  <project path="external/libusb-android" name="vmlinz/libusb" remote="github" revision="master" />

  <!-- add newcircle alpha device profile -->
  <project path="device/marakana/alpha" name="vmlinz/alpha" remote="github" revision="master" />
  <project path="device/marakana/alpha_sdk_addon" name="vmlinz/alpha_sdk_addon" remote="github" revision="master" />

  <!-- add a customized aosp arm device profile -->
  <project path="device/generic/aosp-arm-customized" name="vmlinz/android_device_aosp-arm-customized" remote="github" revision="master" />

</manifest>
